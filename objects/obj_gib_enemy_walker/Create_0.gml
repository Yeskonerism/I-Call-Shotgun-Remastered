randomise();

spd = random_range(2,4);

image_index = irandom_range(0,4);
image_angle = random_range(0,359);

scale = 0.5 + random_range(0,0.25);

image_xscale = scale;
image_yscale = scale;

image_speed = 0;